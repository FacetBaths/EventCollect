{
  "name": "eventcollect",
  "version": "1.0.0",
  "description": "Event lead collection app with LEAP CRM integration",
  "private": true,
  "workspaces": [
    "client",
    "server",
    "shared"
  ],
  "scripts": {
    "dev": "concurrently \"yarn server:dev\" \"yarn client:dev\"",
    "server:dev": "cd server && yarn dev",
    "client:dev": "cd client && yarn dev",
    "build": "yarn shared:build && yarn server:build && yarn client:build",
    "shared:build": "cd shared && yarn build",
    "server:build": "cd server && yarn build",
    "client:build": "cd client && yarn build",
    "install:all": "yarn install && cd client && yarn install && cd ../server && yarn install && cd ../shared && yarn install"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
